# Full-Stack Auth System
A Node.js & Express based authentication system using JWT and Axios.

## Features
- User Signup and Signin
- JWT Authentication Middleware
- Protected User Profile Route

## Setup
1. Clone the repo
2. Run `npm install`
3. Start with `node index.js`
